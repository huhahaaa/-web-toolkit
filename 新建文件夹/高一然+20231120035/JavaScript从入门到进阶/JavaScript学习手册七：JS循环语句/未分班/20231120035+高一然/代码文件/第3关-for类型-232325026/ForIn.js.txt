function mainJs(a){
    a = parseInt(a);
    //请在此处编写代码
    /********** Begin **********/
    var reversed = 0;
    while (a > 0) {
        reversed = reversed * 10 + a % 10;
        a = Math.floor(a / 10);
    }
    return reversed;
    /********** End **********/
}