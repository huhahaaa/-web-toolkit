function mainJs(a,b) {
    //请在此处编写代码
    /********** Begin **********/
    var numA = parseInt(a);
    var numB = parseInt(b);
    var c = numA % numB;

    a = String(a);
    b = String(b);
    c = String(c);
    /********** End **********/

    return a+b+c;
}