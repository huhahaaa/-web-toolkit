var apple = {
    weight:"200克",
    level:"特级",
    locationProvince:"陕西省",
    locationCity:"榆林市"
}
function mainJs(a,b){
    apple[a]= b;
	//请在此处编写代码
	/********** Begin **********/
     var result = "";
      for (var key in apple) {
          if (key.indexOf("location") === 0) {
              result += apple[key];
          }
      }
      return result;
    
	/********** End **********/
}