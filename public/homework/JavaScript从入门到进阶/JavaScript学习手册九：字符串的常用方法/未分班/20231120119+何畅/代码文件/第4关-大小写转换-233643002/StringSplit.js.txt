function mainJs(a,b) {
	//请在此处编写代码
	/********** Begin **********/
    var result = "";
      var i = 0;
      while (i < a.length) {
          if (a.indexOf(b, i) === i) {
              result += b.toUpperCase();
              i += b.length;
          } else {
              result += a.charAt(i);
              i++;
          }
      }
    return result;
      
    
	/********** End **********/
}
