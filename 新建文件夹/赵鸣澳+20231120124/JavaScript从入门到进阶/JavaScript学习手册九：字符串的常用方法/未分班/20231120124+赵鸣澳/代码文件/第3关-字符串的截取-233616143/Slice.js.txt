function mainJs(a,b) {
	//请在此处编写代码
	/********** Begin **********/
    var c = a.indexOf(b)
    
    if(c!=-1){
        return (a.slice(0, c) + a.slice(b.length + c));
        
    }
    return a;
	/********** End **********/
}
